data
test_data
migrations
node_modules
Dockerfile.prebuilt.generated


# Added by cargo

/target
tmp

# e2e fixture recipes stay local; only the generated .osm.pbf is committed
tests/**/*.build-fixture.sh
tests/**/*.boundary-ids.txt
tasks.md