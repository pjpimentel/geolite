1. indentation is always 2 spaces.
2. index names must reflect their purpose, not just what they are (eg: search_by_street_id, not idx_house_numbers_street_id).
3. static sql strings are declared as `const` with a `SQL_` prefix (e.g. `SQL_INSERT`, `SQL_CREATE_TABLE`). a sql string used by a single function is declared inside that function, as its first item; a sql string shared by two or more functions is declared at module level. the ddl (`SQL_CREATE`, `SQL_CREATE_INDEXES`) is always at module level, grouped at the top of the file. multi-line sql must be indented with 2 spaces, each clause on its own line, with a leading and trailing newline inside the string literal. columns and values in insert statements must be listed one per line.
4. database ids must use unsigned integer types, sized as small as possible (e.g. `u8` for admin levels 1–30, `u32` for osm ids if range allows, `u64` only when necessary).
5. json columns must be stored as jsonb: use `jsonb(?n)` in insert/update statements, never plain `?n`. when reading a jsonb column back into rust via `fromsql`, the select must use `json(col)` to convert to text first — `fromsql` implementations use `value.as_str()` and will fail on raw jsonb blobs.
6. planned work is tracked in `changelog.md`, as future versions above the one in progress, each under a `## **planned** - x.y.z` heading with no date. a version holds five items at most: when the last planned version is full, open the next one. a planned item is written as the changelog entry it will become, in english and in one brief sentence (the design detail belongs to the plan of the task, not to the changelog): it starts with one of the action verbs `ADDED`, `REMOVED`, or `MODIFIED` and ends with a purpose clause starting with `so` (e.g. `so a rewritten query cannot lose its index in silence`, `so a base is one file to copy, publish and merge`). new items go to the end of their version (queue, not stack). before executing a planned item, weigh whether it is actually necessary and whether it is redundant with existing functionality — surface the doubt and confirm before implementing. planned items are a backlog of ideas, not a queue of guaranteed work. when adding or rewriting an item, enforce this shape; when grooming the backlog, rewrite legacy items in place to match. when a version is released, its heading takes the date and its items are trimmed to what was done.
7. sql functions must be written in uppercase (e.g. `JSON_EXTRACT`, `COALESCE`, `CAST`, `COUNT`).
8. `src/admin_level/readme.md`, section `the rules`, documents the default inclusion rules per level. keep it in sync with the extraction logic in source — any change to the defaults in `src/admin_level/rules.rs` must be reflected there.
9. test names (file names and scenario names) must always be written in english, regardless of the language used elsewhere in the codebase.
10. dependencies must be added via `cargo add` and removed via `cargo remove`. never edit the `[dependencies]` section of `Cargo.toml` by hand.
11. comments are written only to explain a workaround or a real risk of a performance mistake. everything else — what a thing is, what a function does, why a type exists — must be carried by names and types; a comment that restates the code is an excess and must be removed. module-level and folder-level explanation belongs in the module's `readme.md`, not in a comment block at the top of a file. sql comments inside a `CREATE TABLE` string are part of the ddl text stored in `sqlite_master`, not code comments, and are out of scope for this rule.
12. the primary language of the codebase is english: code, comments, readmes, the changelog and its planned items, commit messages and test names are written in english.
